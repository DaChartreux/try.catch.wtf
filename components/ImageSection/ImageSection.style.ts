import styled from "@emotion/styled";

import THEME from "@styles/theme";

export const ImageSectionStyle = styled.div`
  div {
    text-align: center;
  }

  p {
    text-align: center;
    color: hsla(${THEME.colors["fg-100"]}, 0.8);
    font-weight: 500;
  }
`;
